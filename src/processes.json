{
    "apps": [
        {
            "name": "na-api",
            "script": "./bin/www",
            "merge_logs": true,
            "max_restarts": 20,
            "instances": 4,
            "max_memory_restart": "200M",
            "env": {
                "NODE_ENV": "production",
                "PORT": 3000
            }
        }
    ]
}